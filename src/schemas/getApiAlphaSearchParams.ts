/**
 * Generated by orval
 * Do not edit manually.
 */
import type { GetApiAlphaSearchType } from './getApiAlphaSearchType';
import type { GetApiAlphaSearchListingType } from './getApiAlphaSearchListingType';
import type { GetApiAlphaSearchSort } from './getApiAlphaSearchSort';
import type { GetApiAlphaSearchNsfw } from './getApiAlphaSearchNsfw';

export type GetApiAlphaSearchParams = {
q: string;
type_: GetApiAlphaSearchType;
limit?: number;
/**
 * Only some types are supported for each `type_`.

- `Comments` and `Communities`: `Popular` will return the same results as `All`.
- `Users`: only `Local` will differ from `All`.
- `Posts` and `Url`: These types are equivalent.
 */
listing_type?: GetApiAlphaSearchListingType;
page?: number;
/**
 * Only some sorting options supported for each `type_`. `NewFederated` and `OldFederated` are equivalent to `New` and `Old` respectively for each type except for `Communities`.

- `Comments`: `Scaled` is not supported and `Hot` will be returned instead.
- `Communities`: `Top` sorts that are not present in `/community/list` are equivalent to `Top`.
- `Users`: only `New`, `Old`, and `Top` (by number of posts, all `Top` sorts are equivalent) are supported. Otherwise will be sorted by user id.
- `Posts` and `Url`: These types are equivalent. `TopPosts` and `TopSubscribers` are equivalent to `Top`.
 */
sort?: GetApiAlphaSearchSort;
community_name?: string;
community_id?: number;
minimum_upvotes?: number;
nsfw?: GetApiAlphaSearchNsfw;
};
